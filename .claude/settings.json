{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git status --porcelain | grep '^??.*\\.nix' && echo 'WARNING: untracked .nix files'"
          }
        ]
      }
    ]
  }
}
