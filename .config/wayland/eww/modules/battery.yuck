
(defwidget battery [
  battery status 
  icon_using icon_charging
]
  (box
    :class "battery-box ${battery<20 ? "battery-low":""}"
    :space-evenly false
    :spacing 8

    (label
      :text "${
        status == "charging" ? icon_charging : icon_using
      } ${battery}%"
    )
  )
)
